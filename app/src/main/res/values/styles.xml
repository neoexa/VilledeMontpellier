<resources>

    <!-- Base application theme. -->
    <style name="NeoTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/secondaryColor</item>
        <item name="android:navigationBarColor">@color/primaryDarkColor</item>
    </style>
    <style name="NeoTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="NeoTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="NeoTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
</resources>